apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: nginx-deployment
    fruta: mixirica
  name: nginx-deployment
  namespace: giropops
spec:
  replicas: 8
  selector:
   matchLabels:
    app: nginx-deployment
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: nginx-deployment
    spec:  
      containers:
        - image: nginx:1.14.0
          name: nginx-wow
          resources:
            limits:
              cpu: "0.5"
              memory: 256Mi